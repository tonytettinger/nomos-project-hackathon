# When all three values are present, the API uses ElevenLabs. Otherwise it uses
# the local mock provider so the interface can be exercised without placing calls.
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=
ELEVENLABS_PHONE_NUMBER_ID=
API_PORT=8787
